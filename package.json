{
  "private": true,
  "scripts": {
    "start": "lerna run start --scope=@redux-reducer-injection-messages/* && yarn serve",
    "build": "lerna run --scope=@redux-reducer-injection-messages/* build",
    "serve": "lerna run --scope=@redux-reducer-injection-messages/* --parallel serve",
    "clean": "lerna run --scope=@redux-reducer-injection-messages/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": [
    "@redux-reducer-injection-messages/*"
  ]
}
